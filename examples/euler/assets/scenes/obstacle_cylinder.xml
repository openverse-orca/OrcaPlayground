<!-- 基础障碍几何体：圆柱（static，无 joint，有碰撞）。
     基础尺寸：半径 0.2m，半高 0.4m（总高 0.8m），spawn 时通过 scale 缩放。
     颜色通过 OrcaGymScene.set_material_info 在 spawn 后覆盖。
     用于 Lesson 8 场景布景，为摄像头视频提供丰富的视觉内容。 -->
<mujoco model="obstacle_cylinder">
  <worldbody>
    <body name="obstacle_cylinder" pos="0 0 0">
      <geom name="cylinder_geom" type="cylinder" size="0.2 0.4" rgba="0.4 0.8 0.3 1"/>
    </body>
  </worldbody>
</mujoco>
