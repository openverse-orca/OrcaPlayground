1. 问题描述
- dji_lhcg 模型底部有箱体(mat_026)需要去掉
- 需要在底部改造为 slider 关节机械爪，能扩张收拢抓取箱体

2. 根因分析
- mat_026 是底部灰色大件(中心z=-0.422, rbound=0.604)，属于挂载箱体
- 原模型无抓取机构，需要新增 slider 关节子体

3. 方案设计
- XML: 去掉 mat_026 geom，在 Drone body 下添加两个 slider 关节子体(gripper_left/gripper_right)
- 每个 slider 沿 X 轴滑动，左爪 range=[-0.6, 0.8]，右爪 range=[-0.8, 0.6]
- 爪臂用 box 几何体(0.03×0.03×0.3)，爪尖用 box(0.05×0.05×0.08)红色标识
- 环境: 添加 G/H 键盘控制(收拢/扩张)和 LB/RB 手柄控制
- _update_gripper: 按 target 平滑驱动 qpos，速度限制 1.0 m/s

4. 修改文件清单
- examples/drone_driver/model/dji_lhcg/dji_lhcg.xml
  - 去掉 body_mat_026 geom
  - 新增 gripper default class
  - 新增 gripper_left/gripper_right body + joint + geom + site
- envs/drone/drone_orca_env.py
  - 新增 _gripper_joints/_gripper_enabled/_gripper_target 初始化
  - 新增 G/H 键盘输入和 LB/RB 手柄输入
  - 新增 _update_gripper() 方法
  - reset_model 中重置爪关节
  - step 中调用 _update_gripper
- examples/drone_driver/run_drone_orca.py
  - DRONE_JOINT_SUFFIXES 添加 gripper_left_joint/gripper_right_joint

5. 注意事项
- 爪的碰撞几何体 contype=1, conaffinity=1，可与其他物体接触
- 爪质量 0.3kg×2=0.6kg，对总质量影响较小
- 当前爪控制为开环位置驱动，未做力控/夹持力反馈
- 后续可添加力传感器实现接触检测和自适应夹持
