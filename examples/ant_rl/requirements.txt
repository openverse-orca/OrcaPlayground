# Ant RL 示例依赖
# ⚠️ PyTorch 安装说明：
#   pip install torch 默认安装最新版本，其 CUDA 版本可能不兼容你的 NVIDIA 驱动。
#   请根据你的驱动版本，在 PyTorch 官网选择对应的安装命令：
#     https://pytorch.org/get-started/locally/
#
#   查看驱动版本：nvidia-smi | head -3
#   查看当前 CUDA 版本：python -c "import torch; print(torch.version.cuda)"
# ⚠️ 不要直接 pip install torch，请参考上方说明根据驱动版本选择安装命令
# torch (需手动安装，版本取决于驱动，见上方说明)

# RLlib APPO 训练
ray[rllib]>=2.54.0
