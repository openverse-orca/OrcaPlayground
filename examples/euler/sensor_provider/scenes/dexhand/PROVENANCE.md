# 灵巧手示例模型记录

- 完整灵巧手 XML、网格和纹理由用户提供，用户确认是自有模型资产；本例不声明第三方厂商或产品关联。
- 示例仅携带 XML 引用的 36 个网格/纹理文件。
- 输入整机 XML 由用户提供，原文本 SHA256 为 `874515320c1032141575f7b49c200d0d94c551254a5664a688b4e9b4122692b2`，不能仅用上游提交号替代这份输入文本的身份。
- 示例修改：保留原机械结构、mesh/纹理、site、碰撞及执行器；将原生传感器/插件声明替换为 Orca 标准 custom 声明；资源路径调整为随包的相对路径。
- 抓取演示修正：圆柱的 `<joint type="free"/>` 改为 `<freejoint/>`，避免继承默认关节阻尼 1；手指关节及执行器限力不变。这是分发场景中的显式物理参数修正。
- 本次分发直接提供已组装的 `scene.xml` 与配套资源；修改模型时应在此记录输入来源和物理参数变化。
- 本目录的 `scene.xml` 与 `meshes/` 是完整场景及其配套资源，应一起保存和分发。

该模型用于验证接入与物理数据传递，不表示真实手部参数校准或传感器精度认证。算法样板采用独立的教学近似公式，不对应实际产品算法。
