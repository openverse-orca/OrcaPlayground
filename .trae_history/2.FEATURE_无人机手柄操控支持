1. 问题描述
   用户已适配好 dji_lhcg 新模型，当前无人机环境仅支持键盘操控，需要增加 Xbox 手柄操控方式。
   运行后发现：1) 自动轨迹/阻尼占大部分控制权；2) WASD 完全没反应。

2. 根因分析
   - DroneOrcaEnv._read_keyboard_command() 硬编码为仅读取键盘输入，无手柄支持
   - dji_lhcg 是 15kg 大无人机（惯性矩 I_rp≈6.25 kg·m²），但控制参数是按 ~1.5kg 小无人机设计的：
     - demo_rotor_bias=(25,25,25,25)：桨速偏置产生自动轨迹
     - 姿态力矩上限 trp_max=0.08 N·m：对 I=6.25 → 倾转 16° 需 6+ 秒，WASD 无反应
     - quad_world_xy_stick_force_factor=0.003：直接水平力仅 ~0.44N，对 15kg 无效
     - 阻尼参数过高：zero_cmd_xy_hold_k=0.80, world_xy_velocity_damping=0.80
     - thrust_cmd_scale_over_hover=0.12：升降杆量几乎无效
     - reset 推力渐入 1.5s + 最小稳定窗口 0.8s 太长

3. 方案设计
   手柄支持：
   - 在 drone_orca_env.py 中添加 ControlDevice 枚举类（KEYBOARD/XBOX）
   - _read_keyboard_command() 拆分为路由 + 手柄/键盘两个实现
   - close() 覆写清理手柄资源
   - run_drone_orca.py 添加 --ctrl-device 参数并透传

   WASD 修复（核心：让直接水平力成为主要响应机制，而非依赖慢速倾转）：
   - quad_world_xy_stick_force_factor 0.003→0.10：满杆直接力 14.7N → 0.98 m/s² 即时响应
   - demo_rotor_bias (25,25,25,25)→(0,0,0,0)：取消自动轨迹
   - attitude_kp_scale 1.10→2.50, attitude_kd_scale 1.05→2.00：提高姿态响应
   - attitude_torque_limit_scale 1.30→6.00, idle 1.60→4.50：匹配大惯量
   - max_body_torque_norm 2.50→3.50：允许更大偏航力矩
   - angular_drag_torque_axis_max 1.20→1.80：匹配大惯量角阻尼
   - thrust_cmd_scale_over_hover 0.12→0.30：升降杆量有效
   - tau_yaw_over_hover 0.004→0.020：偏航灵敏
   - max_tilt_deg 12→16：允许更大倾角
   - 阻尼全面降低：linear_xy/z 0.80→0.30, world_xy_velocity_damping 0.80→0.25
   - reset 推力渐入 1.5s→0.4s，最小稳定窗口 0.8s→0.15s

4. 修改文件清单
   - envs/drone/drone_orca_env.py
     - 新增 import XboxJoystick, XboxJoystickManager
     - 新增 ControlDevice 枚举
     - __init__() 新增 ctrl_device 参数及设备初始化逻辑
     - _read_keyboard_command() 拆分为路由 + 手柄/键盘两个实现
     - 新增 close() 覆写清理手柄资源
     - reset_model() 新增 _last_a_button_state 重置
   - examples/drone_driver/run_drone_orca.py
     - register_env() / run_simulation() 新增 ctrl_device 参数并透传
     - argparse 新增 --ctrl-device 选项
     - 启动时输出控制设备信息日志
   - envs/drone/drone_aero_config.py
     - _DJI_LHCG_DRAG_CONFIG: quad_world_xy_stick_force_factor 0.003→0.10, 降低阻尼, 提高力矩上限
     - dji_lhcg FullModeControlConfig: 取消 demo_rotor_bias, 大幅提高控制增益和力矩上限, 缩短 reset 过渡

5. 注意事项
   - WASD 响应机制：直接水平力（即时）+ 倾转分力（慢但持续），前者为主
   - 15kg 机体倾转仍需 ~1-2 秒，但直接力已提供即时响应
   - 阻尼降低后松杆漂移会更多，可适当回调 zero_cmd_xy_hold_k
   - 手柄 A 键 = 键盘 Space（重置），未检测手柄时自动降级为键盘
